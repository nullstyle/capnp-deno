# WASM Host ABI

The canonical ABI specification is maintained in the vendored capnp-zig docs:

- `vendor/capnp-zig/docs/wasm_host_abi.md`

This file exists so repository-local doc links remain stable.
