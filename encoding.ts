/**
 * Public encoding-only entrypoint.
 *
 * Import from `@nullstyle/capnp/encoding` when you only need schema/serde
 * encode/decode helpers without RPC runtime APIs.
 */

export * from "./src/encoding/mod.ts";
