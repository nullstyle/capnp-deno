#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec deno run --allow-read --allow-write --allow-run=capnp \
  "$ROOT_DIR/tools/capnpc-deno/main.ts" "$@"
